# URL of your running backend (the Express server in ../backend)
VITE_BACKEND_URL=

# Clerk publishable key (Dashboard -> API Keys). Use the SAME Clerk
# application as the backend's CLERK_SECRET_KEY/CLERK_PUBLISHABLE_KEY,
# otherwise session tokens issued to the frontend won't verify server-side.
VITE_CLERK_PUBLISHABLE_KEY=
